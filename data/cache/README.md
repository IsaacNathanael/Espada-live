# Environmental cache

Run `scripts/sync_environment.ps1` to place the latest normalized environmental response here.

The cache is deliberately not pre-labelled as real unless it was downloaded successfully. `auto` mode tries live Open-Meteo data, then an existing real cache, then a clearly labelled synthetic fallback.
